version: '3.8'
services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=sqlite:///gwaro.db
      - SECRET_KEY=change-me
    volumes:
      - ./logs:/app/logs
      - ./database:/app/database
    restart: unless-stopped
